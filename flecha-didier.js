const funcionFlecha = (arreglo) => {
  arreglo.map((item) => {
    if (item.length > 5) {
      console.log(item);
    }
  });
};

// Estructura de función flecha
// const funcionFlecha = (arreglo) => {

// }

// Estructura de el .map
// arreglo.map((item) => {

// })
