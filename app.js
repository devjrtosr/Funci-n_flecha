let EquiposDeFutbol=["Liga", "Cartago", "Saprisa", "Heredia"];

const  recibirArreglo=(equipos) => {
    EquiposDeFutbol.map(equipo => {
        if (equipo.Length>5) {
            console.log(equipo);
        }
    } )

}