const marcasTenis=["nike","adidas","puma","champion","fila"];

const recibirArreglo=(marcas)=>{
    marcas.map((marca) => {
        if(item.length>5){
            console.log(marca);
        };
    });
};
recibirArreglo(marcasTenis);